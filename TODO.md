# TODO: 未対応 SQL 機能

現状サポートしている機能との差分として、SQL:2023 (ISO/IEC 9075:2023) の標準仕様を軸に整理する。
oresql 独自の拡張や実装上の項目は末尾にまとめる。

---

## Part 2: SQL/Foundation — データ型 (E011–E021)

- [x] INTEGER / INT (E011)
- [x] TEXT / CHARACTER VARYING
- [x] BOOLEAN (E011)
- [x] FLOAT / REAL / DOUBLE PRECISION (E011)
- [ ] DECIMAL / NUMERIC (E011) — 任意精度の正確な数値型
- [ ] DATE / TIME / TIMESTAMP (E011) — 日時型
- [ ] TIME WITH TIME ZONE / TIMESTAMP WITH TIME ZONE (E011)
- [ ] INTERVAL (E011) — 時間間隔型
- [ ] VARCHAR(n) — 長さ制限付き文字列 (E021)
- [ ] CHAR(n) — 固定長文字列 (E021)
- [ ] BLOB / BINARY LARGE OBJECT (E011)
- [ ] CLOB / CHARACTER LARGE OBJECT (E011)
- [ ] ARRAY 型 (S091) — `INTEGER ARRAY[10]`
- [x] JSON 型 (T801, SQL:2016+) — SQL/JSON パスサポート

## Part 2: SQL/Foundation — スキーマ定義 DDL (E101–E153)

### テーブル操作

- [x] CREATE TABLE (E101)
- [x] DROP TABLE (E101)
- [x] TRUNCATE TABLE (E182, SQL:2008+)
- [x] ALTER TABLE ADD COLUMN / DROP COLUMN (E101)
- [ ] ALTER TABLE RENAME TABLE / RENAME COLUMN (E101)
- [ ] IF EXISTS / IF NOT EXISTS（CREATE TABLE IF NOT EXISTS, DROP TABLE IF EXISTS）(T191)
- [ ] CREATE TABLE AS SELECT (CTAS) — クエリ結果からテーブル作成
- [ ] TEMPORARY TABLE — セッション/トランザクションスコープの一時テーブル (F531)

### 制約 (E141)

- [x] PRIMARY KEY 制約 (E141)
- [x] NOT NULL 制約 (E141)
- [x] UNIQUE 制約 (E141)
- [x] DEFAULT 値 (E141)
- [ ] CHECK 制約 (E141) — `CHECK (age >= 0)`
- [ ] FOREIGN KEY 制約 / REFERENCES (E141)
  - [ ] ON DELETE CASCADE / SET NULL / SET DEFAULT / RESTRICT
  - [ ] ON UPDATE CASCADE / SET NULL
- [ ] GENERATED ALWAYS AS (計算カラム) (T171, SQL:2003+)

### インデックス

- [x] CREATE INDEX / DROP INDEX（複合インデックス対応）
- [x] CREATE INDEX ... USING GIN（TEXT 全文検索用転置インデックス）
- [ ] CREATE UNIQUE INDEX
- [ ] 部分インデックス（WHERE 付き）
- [ ] 式インデックス（関数インデックス）

### シーケンス

- [ ] AUTO INCREMENT / SERIAL
- [ ] CREATE SEQUENCE / DROP SEQUENCE (T176)
- [ ] GENERATED ALWAYS AS IDENTITY / GENERATED BY DEFAULT AS IDENTITY (T174, SQL:2003+)

## Part 2: SQL/Foundation — データ操作 DML (E101–E152)

### INSERT

- [x] INSERT INTO ... VALUES (E101)
- [x] INSERT でカラムリストを指定 (`INSERT INTO t (col1, col2) VALUES (...)`) (E101)
- [x] INSERT ... SELECT (E101)
- [ ] INSERT ... DEFAULT VALUES
- [ ] INSERT ... ON CONFLICT / ON DUPLICATE KEY UPDATE（UPSERT）— MERGE 文で標準化 (F312)
- [ ] 複数行 VALUES (`INSERT INTO t VALUES (1,'a'), (2,'b')`)

### UPDATE / DELETE

- [x] UPDATE ... SET ... WHERE (E101)
- [x] DELETE FROM ... WHERE (E101)
- [ ] UPDATE ... FROM（他テーブルを参照した更新）
- [ ] DELETE ... USING（他テーブルを参照した削除）

### MERGE (F312, SQL:2003+)

- [ ] MERGE INTO ... USING ... ON ... WHEN MATCHED / WHEN NOT MATCHED

## Part 2: SQL/Foundation — クエリ式 (E051–E071)

### SELECT 基本

- [x] SELECT ... FROM (E051)
- [x] DISTINCT (E051)
- [ ] DISTINCT ON (col, ...) — PostgreSQL 拡張
- [x] テーブル別名 (`FROM users u`) (E051)
- [x] カラム別名 (`SELECT col AS alias`) (E051)

### WHERE / 述語 (E061)

- [x] 比較演算子 (`=`, `!=`, `<`, `>`, `<=`, `>=`) (E061)
- [x] AND / OR 論理演算子 (E061)
- [x] NOT（一般的な否定）(E061)
- [x] IN (`WHERE id IN (1, 2, 3)`) (E061)
- [x] BETWEEN (`WHERE id BETWEEN 1 AND 10`) (E061)
- [x] LIKE (`WHERE name LIKE '%alice%'`) (E061)
- [x] IS NULL / IS NOT NULL (E061)
- [ ] IS DISTINCT FROM / IS NOT DISTINCT FROM (T151, SQL:2003+) — NULL 安全な比較
- [ ] 複数カラム IN (`WHERE (a, b) IN ((1,2), (3,4))`) — 行値コンストラクタ (T141)
- [ ] ANY / SOME / ALL 比較演算子 (`WHERE x > ANY (SELECT ...)`) (E061)
- [ ] SIMILAR TO / 正規表現マッチ (F421)

### 演算子

- [ ] 文字列連結演算子（`||`）(E021)
- [ ] 剰余演算子（`%`）※ MOD 関数は対応済み

### ORDER BY / LIMIT (E071)

- [x] ORDER BY (ASC / DESC) (E071)
- [ ] ORDER BY ... NULLS FIRST / NULLS LAST (F471, SQL:2003+)
- [x] LIMIT / OFFSET — 標準は FETCH FIRST ... ROWS ONLY (F858, SQL:2008+)
- [ ] FETCH FIRST n ROWS ONLY / OFFSET n ROWS (F857–F858, SQL:2008+) — 標準構文

### GROUP BY / HAVING (E051)

- [x] GROUP BY (E051)
- [x] HAVING (E051)
- [ ] GROUP BY ROLLUP / CUBE / GROUPING SETS (T431, SQL:1999+)

### JOIN (E071)

- [x] INNER JOIN ... ON (E071)
- [x] LEFT JOIN (E071)
- [x] RIGHT JOIN (E071)
- [x] CROSS JOIN (E071)
- [x] JOIN ... USING (col1, col2) (E071)
- [ ] FULL OUTER JOIN (E071)
- [ ] NATURAL JOIN (E071)
- [ ] LATERAL JOIN (T491, SQL:2003+) — 相関サブクエリを FROM 句で使用

### 集合演算 (E071)

- [x] UNION / UNION ALL (E071)
- [x] INTERSECT (E071)
- [x] EXCEPT (E071)

### サブクエリ (E061)

- [x] スカラーサブクエリ (`SELECT (SELECT ...)`) (E061)
- [x] IN サブクエリ (`WHERE id IN (SELECT ...)`) (E061)
- [x] EXISTS サブクエリ (E061)
- [x] FROM サブクエリ (`SELECT * FROM (SELECT ...) AS sub`) (E061)
- [x] 相関サブクエリ (E061)

### CTE 共通テーブル式 (T121, SQL:1999+)

- [x] WITH ... AS（非再帰 CTE）(T121)
- [x] WITH RECURSIVE（再帰 CTE、UNION / UNION ALL 対応）(T131)
- [ ] CTE を UPDATE / DELETE で使用 — CTE in DML (T121)

## Part 2: SQL/Foundation — 組み込み関数

### 集約関数 (E091)

- [x] COUNT (E091)
- [x] SUM (E091)
- [x] AVG (E091)
- [x] MIN (E091)
- [x] MAX (E091)
- [ ] LISTAGG / STRING_AGG / GROUP_CONCAT (T625, SQL:2016+) — 文字列集約
- [x] COUNT(DISTINCT col) — DISTINCT 付き集約
- [ ] FILTER (WHERE ...) 句 — 集約のフィルタリング (T612, SQL:2003+)

### ウィンドウ関数 (T611, SQL:2003+)

- [x] ROW_NUMBER, RANK, DENSE_RANK (T611)
- [x] SUM / COUNT / AVG / MIN / MAX OVER (T611)
- [ ] NTILE, PERCENT_RANK, CUME_DIST (T611)
- [ ] LAG / LEAD (T611) — 前後の行を参照
- [ ] FIRST_VALUE / LAST_VALUE / NTH_VALUE (T611)
- [ ] ROWS / RANGE / GROUPS フレーム指定 (T612)
- [ ] EXCLUDE 句（EXCLUDE CURRENT ROW 等）(T613, SQL:2011+)

### 数値関数

- [x] ABS, ROUND, MOD, CEIL, FLOOR, POWER (E011)
- [ ] LN, EXP, SQRT, LOG10 (E011)
- [ ] GREATEST / LEAST — 非標準だが広く使われる

### 文字列関数 (E021)

- [x] LENGTH, UPPER, LOWER, SUBSTRING, TRIM, CONCAT (E021)
- [ ] POSITION (E021) — `POSITION('x' IN col)`
- [ ] OVERLAY (E021) — 文字列の部分置換
- [ ] TRANSLATE — 文字の変換
- [ ] REPLACE — 文字列の置換
- [ ] LEFT / RIGHT — 先頭/末尾の部分文字列

### 日時関数 (E011)

- [ ] CURRENT_DATE / CURRENT_TIME / CURRENT_TIMESTAMP (E011)
- [ ] EXTRACT (YEAR / MONTH / DAY / HOUR / MINUTE / SECOND FROM ...) (E011)
- [ ] DATE_ADD / DATE_DIFF — 非標準だが広く使われる
- [ ] INTERVAL 演算 — `timestamp + INTERVAL '1 day'`

### 条件式

- [x] CASE WHEN ... THEN ... ELSE ... END (E029)
- [x] COALESCE / NULLIF (E011)
- [x] CAST（型変換）(E021)
- [ ] IFNULL / IIF — 非標準（MySQL/SQLite 拡張）

## Part 4: SQL/PSM — トランザクション (E151)

- [ ] BEGIN / START TRANSACTION (E151)
- [ ] COMMIT (E151)
- [ ] ROLLBACK (E151)
- [ ] SAVEPOINT / RELEASE SAVEPOINT (E153)
- [ ] SET TRANSACTION ISOLATION LEVEL (E151)
  - [ ] READ UNCOMMITTED / READ COMMITTED / REPEATABLE READ / SERIALIZABLE

## Part 11: SQL/Schemata — 情報スキーマ (E131)

- [ ] INFORMATION_SCHEMA.TABLES (E131)
- [ ] INFORMATION_SCHEMA.COLUMNS (E131)
- [ ] INFORMATION_SCHEMA.TABLE_CONSTRAINTS (E131)
- [ ] INFORMATION_SCHEMA.KEY_COLUMN_USAGE (E131)

## SQL:2016+ — SQL/JSON (T801–T830)

- [x] JSON 型 (T801)
- [ ] JSON_OBJECT / JSON_ARRAY — JSON 値の生成 (T802)
- [ ] JSON_VALUE / JSON_QUERY — JSON からのスカラー/構造の抽出 (T803)
- [ ] JSON_EXISTS — JSON パスの存在確認 (T804)
- [ ] JSON_TABLE — JSON を関係テーブルに変換 (T805)
- [ ] IS JSON 述語 (T801)

## SQL:2023 新機能

- [ ] ANY_VALUE 集約関数 — GROUP BY で非集約カラムの任意の値を取得
- [ ] GREATEST / LEAST — 標準化（SQL:2023 で追加）
- [ ] LPAD / RPAD — 文字列パディングの標準化
- [ ] LTRIM / RTRIM — TRIM の省略形の標準化
- [ ] 多値割り当て — `SET (a, b) = (SELECT x, y FROM ...)`
- [ ] ORDER BY 内の集約 — `ORDER BY SUM(col)`
- [ ] Property Graph Queries (SQL/PGQ) — グラフクエリ構文

---

## oresql 独自 — データベース管理

- [x] CREATE DATABASE / DROP DATABASE
- [x] USE DATABASE
- [x] SHOW DATABASES
- [x] SHOW TABLES
- [x] クロスデータベース テーブル参照 (`db.table` 構文)
- [ ] DESCRIBE / SHOW COLUMNS（テーブル構造の表示）

## oresql 独自 — VIEW

- [ ] CREATE VIEW / DROP VIEW (E151)

## oresql 独自 — クエリ分析

- [x] EXPLAIN（実行計画の表示）
- [ ] EXPLAIN ANALYZE（実行計画 + 実測値）

## oresql 独自 — BTree インデックス最適化

現状の BTree インデックス活用状況と未対応のケースを整理する。

### 対応済み

- [x] WHERE 等値検索 (`WHERE col = val`) — インデックスルックアップ
- [x] WHERE 範囲検索 (`WHERE col > val`) — `OrderedRangeScan`
- [x] WHERE IN (`WHERE col IN (...)`) — 複数キーインデックスルックアップ
- [x] WHERE LIKE 前方一致 (`WHERE col LIKE 'prefix%'`) — プレフィックス範囲スキャン
- [x] WHERE 複合インデックス — プレフィックス等値 + 範囲条件
- [x] WHERE OR 条件 — Index Merge Union
- [x] ORDER BY + LIMIT on indexed col — インデックス順スキャン
- [x] JOIN inner テーブル equi-join カラム — インデックスネステッドループ
- [x] JOIN inner テーブル LocalWhere — 複合インデックス活用
- [x] MIN/MAX on indexed col (WHERE なし) — B-tree 先頭/末尾の O(log N) ルックアップ
- [x] COUNT(*) (WHERE なし) — `RowCount()` による O(1) カウント
- [x] Covering Index — composite key から直接カラム値デコード（Disk 向け）
- [x] PK Covering Index — PK カラムのみ参照時に行データ読み取りスキップ（Disk 向け）
- [x] WHERE + LIMIT ストリーミング — インデックス走査の早期打ち切り

### 未対応

- [x] GROUP BY on indexed col — インデックス順走査による1パスグループ化（現状はハッシュマップ）
- [ ] DISTINCT on indexed col — インデックス順走査による重複スキップ（現状はハッシュ dedup）
- [ ] COUNT(*) WHERE indexed_col = val — インデックスのマッチキー数カウント（行フェッチ省略）
- [ ] COUNT(DISTINCT col) on indexed col — インデックス順走査による一意値カウント
- [ ] ORDER BY indexed col (LIMIT なし) — ソート省略（現状は LIMIT ありのみ最適化）
- [ ] GROUP BY + ORDER BY 同一カラム — インデックス順グループ化でソート不要化
- [ ] Correlated subquery on indexed col — 相関条件カラムのインデックスルックアップ（現状はフルスキャン）
